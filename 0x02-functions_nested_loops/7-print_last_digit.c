#include "main.h"
/**
*print_last_digit - prints the last digit
*@m: the parameter
*Return: the last digit of a figure
*/
int print_last_digit(int m)
{
int lastdigit;
lastdigit = m % 10;
return (lastdigit);
}
