#include "main.h"
#include <stdio.h>
/**
*mul - it multiplies given integers
*@a: a cvariable to be used
*@b: a variable to be used
*Return: the product
*/
int mul(int a, int b)
{
int product;
product = a * b;
return (product);
}
